function LoginPage() {
    return (
        <section>
            <h2>Log In</h2>

            <p>The login form will send user credentials to the FastAPI backend and
                receive an access token after succeful authentication.</p>
        </section>
    )
}

export default LoginPage